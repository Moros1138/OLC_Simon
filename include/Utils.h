#pragma once

#include "olcPixelGameEngine.h"

void DrawStringCenter(olc::PixelGameEngine* pge, olc::vi2d pos, const std::string text, olc::Pixel fgColor = olc::WHITE, olc::Pixel bgColor = olc::BLANK);
