#pragma once

#include "olcPixelGameEngine.h"

void DrawStringCenter(olc::PixelGameEngine* pge, olc::vi2d pos, std::string text, const olc::Pixel fgColor = olc::WHITE, const olc::Pixel bgColor = olc::BLANK);

